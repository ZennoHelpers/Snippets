string test = "{\"message\":8,\"contact\":1}"; //данные в json
project.Json.FromString(test); //Распаршивание данных json в переменные зеннопостера (вкладка json)
return project.Json.message; //Парсим значение конкретной строки
